name: Bitget 第七个工作流 - 4小时级别扫描

on:
  schedule:
    # 每天北京时间 4:00, 8:00, 12:00, 16:00, 20:00 运行
    # 对应 UTC 时间 20:00, 0:00, 4:00, 8:00, 12:00
    - cron: '0 20,0,4,8,12 * * *'
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - run: pip install -r requirements.txt
      - name: Run 7th workflow scan
        run: python scanner_7th_workflow.py
